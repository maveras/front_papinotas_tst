to run this proyect first we have to clone the backend repository located at:
https://github.com/maveras/backpapinotastst

run the backend in the port 3000,

In order to run the Front, we must clone it and run:
yarn install
yarn start
